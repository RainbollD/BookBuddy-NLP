import re
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM,  GPT2Tokenizer, T5ForConditionalGeneration
import torch
from transformers import GPT2Tokenizer, T5ForConditionalGeneration

class Summarize:
    def __init__(self, text: str, model_name: str = "csebuetnlp/mT5_multilingual_XLSum"):
        self.tokenizer = AutoTokenizer.from_pretrained(model_name)
        self.model = AutoModelForSeq2SeqLM.from_pretrained(model_name)
        self.text = text

    def get_summary(self):
        WHITESPACE_HANDLER = lambda k: re.sub('\s+', ' ', re.sub('\n+', ' ', k.strip()))

        input_ids = self.tokenizer(
            [WHITESPACE_HANDLER(self.text)],
            return_tensors="pt",
            padding="max_length",
            truncation=True,
            max_length=512
        )["input_ids"]

        output_ids = self.model.generate(
            input_ids=input_ids,
            max_length=84,
            no_repeat_ngram_size=2,
            num_beams=4
        )[0]

        summary = self.tokenizer.decode(
            output_ids,
            skip_special_tokens=True,
            clean_up_tokenization_spaces=False
        )

        print(summary)


if __name__ == "__main__":
    input_text = """
    Конечно! Вот большой и развернутый текст про лес — с описанием природы, функций леса, его обитателей и значения для человека и планеты:

---

## 🌲 Лес — живое сердце природы

Лес — это сложная и саморегулирующаяся экосистема, в которой деревья, кустарники, травы, животные, грибы и микроорганизмы живут в тесном взаимодействии друг с другом. Он покрывает около **30% поверхности суши** на Земле и играет одну из ключевых ролей в поддержании экологического баланса на планете.

### 🌳 Структура леса

Лес имеет вертикальную структуру, состоящую из нескольких ярусов:

1. **Древостой (верхний ярус)** — высокие деревья: сосна, ель, дуб, кедр.
2. **Подлесок (средний ярус)** — кустарники: рябина, можжевельник, лещина.
3. **Травяной покров (нижний ярус)** — папоротники, мхи, цветковые растения.
4. **Почвенный ярус** — грибы, бактерии, мицелий, черви.

Эта многоярусная структура позволяет множеству видов находить своё место в лесу и минимизирует конкуренцию между ними за свет, влагу и питательные вещества.

---

### 🌱 Биологическое разнообразие

Лес — дом для **миллионов видов живых организмов**. В одном только гектаре тропического леса может обитать больше видов, чем во всей Европе. В умеренных широтах в лесах живут лоси, медведи, волки, зайцы, белки, а также множество птиц — от дятлов до филинов.

Многие растения и животные имеют **симбиотические отношения**. Например, грибы помогают деревьям усваивать воду и минеральные вещества, а взамен получают сахара, синтезируемые в листьях.

---

### 🌍 Экологическое значение леса

Лес выполняет множество важнейших функций:

* **Производство кислорода** — леса называются "лёгкими планеты". Один взрослый дуб за год производит до 100 кг кислорода.
* **Поглощение углекислого газа (CO₂)** — помогает замедлить глобальное потепление.
* **Регуляция климата** — леса влияют на влажность, осадки, температуру воздуха.
* **Фильтрация воды и воздуха** — корни деревьев очищают воду, а листья задерживают пыль и вредные газы.
* **Предотвращение эрозии почвы** — корневая система укрепляет грунт.
* **Среда обитания** — убежище для животных и растений.

---

### 🧬 Роль леса для человека

С древних времён лес был для человека источником:

* **Древесины** — для строительства, отопления, производства бумаги.
* **Пищи** — ягоды, орехи, грибы, дичь.
* **Лекарств** — многие растения имеют лечебные свойства (например, зверобой, чага, мята).
* **Культуры и вдохновения** — лес занимает особое место в мифах, сказках, литературе и изобразительном искусстве.

В современном мире человек также использует лес для **рекреации** — туризма, прогулок, отдыха на природе, что особенно важно в эпоху урбанизации и стресса.

---

### ⚠️ Угрозы для лесов

Несмотря на свою устойчивость, леса уязвимы:

* **Вырубка лесов** — особенно в тропиках (например, Амазонка) приводит к потере биоразнообразия.
* **Пожары** — часто вызваны человеком: неосторожность, поджоги, климатические изменения.
* **Загрязнение** — кислотные дожди, выбросы вредных веществ.
* **Инвазивные виды и болезни** — поражают местные растения и нарушают баланс экосистем.

---

### 🌿 Будущее лесов — в наших руках

Защитить лес — значит защитить саму жизнь. Современные экологи и учёные разрабатывают методы **восстановления лесов**, создаются заповедники, ведётся работа по лесовосстановлению, используется **устойчивое лесопользование**, внедряются технологии спутникового мониторинга.

Каждый человек может внести вклад:

* Сокращать использование бумаги
* Сажать деревья
* Участвовать в экологических инициативах
* Поддерживать ответственных производителей

---

## 🌳 Заключение

Лес — это не просто совокупность деревьев. Это живой организм, сложная система, полная красоты, гармонии и смысла. Он учит нас взаимодействию, терпению и взаимозависимости. Сохранив лес, мы сохраняем будущее — не только своё, но и всех живых существ на планете.

---

Если нужно, могу сделать **краткое содержание**, **извлечь сущности**, **сгенерировать вопросы по этому тексту** или оформить его в виде статьи для блога / презентации.
"""


    tokenizer = GPT2Tokenizer.from_pretrained('RussianNLP/FRED-T5-Summarizer', eos_token='</s>')
    model = T5ForConditionalGeneration.from_pretrained('RussianNLP/FRED-T5-Summarizer')
    device = 'cuda'
    model.to(device)

    input_ids = torch.tensor([tokenizer.encode(input_text)]).to(device)
    outputs = model.generate(input_ids, eos_token_id=tokenizer.eos_token_id,
                             num_beams=5,
                             min_new_tokens=17,
                             max_new_tokens=200,
                             do_sample=True,
                             no_repeat_ngram_size=4,
                             top_p=0.9)
    print(tokenizer.decode(outputs[0][1:]))

    # print result: Старик Иван живёт размеренной жизнью в деревне, выращивая овощи и находя радость в простых вещах.


